# sample API written in Golang

## Need to install runtime for dev

*) Install golang  version 1.18 or higher

## Test & run 

```bash
go run main.go
```

## Build Binnary file

```bash
go build -o sample-api
```